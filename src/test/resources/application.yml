spring:
  datasource:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
      database: 0
      password:
      timeout: 5000

      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 2
          max-wait: 5000ms